#include <stdio.h>
#include <stdlib.h>
int main(){
    // Imprime a mensagem na tela
    printf("Hello, World!");

    // Retorna 0 para indicar que o programa foi executado com sucesso
    return 0;
}
